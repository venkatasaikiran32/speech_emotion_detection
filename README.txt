# Speech Emotion Recognition with Convolutional Neural Network  

## 📌 Project Overview  
This project focuses on **Speech Emotion Recognition (SER)** using a **Convolutional Neural Network (CNN)**. It combines audio features from the **RAVDESS** and **TESS** datasets to classify emotions from speech signals.  

## 📂 Folder Structure  

- **`Speech Emotion Recognition with Convolutional Neural Network - Sai Kiran - final.ipynb`**  
  - Jupyter Notebook containing the complete project code.  

- **`Data Sets/`** *(Final processed datasets used for training the CNN)*  
  - `emotion_capstone_final_dataframe_sai_kiran.csv` → Combined dataset (RAVDESS + TESS).  
  - `emotion_capstone_final_ravdess_dataframe_sai_kiran.csv` → Cleaned and transformed RAVDESS dataset.  
  - `emotion_capstone_final_tess_dataframe_sai_kiran.csv` → Cleaned and transformed TESS dataset.  

- **`CNN Model/`** *(Trained model files)*  
  - `capstone_project_emotion_detection_final_version.h5` → Trained CNN model weights.  
  - `capstone_project_emotion_detection_final_version.json` → Model architecture in JSON format.  

- **`Report/`** *(Final project documentation)*  
  - `Speech Emotion Recognition with Convolutional Neural Network - Sai Kiran Report` → Detailed report on methodology, dataset, model, and results.  

## 🚀 How to Run the Project  

1. Clone this repository or download the files.  
2. Install dependencies:  
   ```bash
   pip install tensorflow numpy pandas librosa matplotlib

